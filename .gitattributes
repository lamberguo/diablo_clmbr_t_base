# Large adapter weight files (use Git LFS when pushing to GitHub).
weights/adapters/**/adapter_model.bin filter=lfs diff=lfs merge=lfs -text
weights/adapters/**/adapter_model.pt filter=lfs diff=lfs merge=lfs -text
